CREATE TABLE Person (
                        id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                        full_name varchar(100) NOT NULL UNIQUE,
                        year_of_birth int NOT NULL
);

CREATE TABLE Book (
                      id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                      title varchar(100) NOT NULL,
                      author varchar(100) NOT NULL,
                      year int NOT NULL,
                      person_id int REFERENCES Person(id) ON DELETE SET NULL
);

insert into person (full_name, year_of_birth) VALUES ('Pablo Escabaro', 1980);

insert into book (title, author, year, person_id) VALUES ('1984', 'Oruel', 1984, 1);

alter table book add column created_at timestamp;

UPDATE Book SET created_at='2023-02-02 08:00:00' WHERE id=1;
